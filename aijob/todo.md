# 待完善任务清单

更新时间：2026-04-03

## 1. EXCEL 导入前预检模式
- 增加“只校验不导入”的预检接口。
- 在正式写库前统计：可导入条数、失败条数、失败原因。
- 前端在用户正式导入前先展示预检结果，减少误导入。

## 2. 导入失败智能修正建议
- 对 `handling_department`、`project`、`approval_status` 等选项字段，失败时给出最接近的候选值。
- 在失败明细导出文件中增加“建议修正值”列，方便用户直接修改后二次导入。
- 可考虑基于当前 `_match_option_value` 的相似匹配结果输出建议值。

## 3. EXCEL 导入完整实测
- 已对真实文件 `backend/instance/合同审批_20260214_084913_406.xls` 完成表头识别与前 20 行 dry-run。
- 后续需要做一次完整导入实测，包括：
  - 导入成功后的列表数据核对
  - 重复编号跳过验证
  - 失败明细自动下载验证
  - 修正后再次导入验证

## 4. 导入失败明细的生命周期优化
- 当前失败明细文件通过内存字典 `_IMPORT_ERROR_REPORTS` 暂存，下载一次后即删除。
- 后续可考虑：
  - 增加过期清理机制
  - 增加按用户隔离
  - 改为落地到临时目录，避免进程重启后丢失

## 5. 后端服务启动问题排查
- 当前上下文中多次 `python run.py` 启动失败，但未完成根因排查。
- 需要单独确认：
  - 是端口占用、环境变量、依赖还是运行目录问题
  - 前后端联调时的标准启动步骤
  - 是否要补一个统一启动脚本或任务配置

## 6. 金额精度链路回归验证
- SQLite 已迁移为 `contracts.amount NUMERIC(20, 8)`。
- 仍建议补一轮完整回归：
  - 新建合同输入 8 位以上小数并保存
  - 列表页展示精度检查
  - 首页汇总金额检查
  - EXCEL 导入金额精度检查
  - AI 解析金额写库与返回精度检查

## 7. 导入模板增强
- 当前模板已提供固定表头和填写说明。
- 后续可继续增强：
  - 增加示例数据行
  - 为选项字段添加候选值说明页
  - 对日期、金额列添加格式提示或单元格注释

## 8. 文档补充
- README 或 docs 中补充 EXCEL 导入说明：
  - 支持的文件格式
  - 必填列说明
  - 模板下载入口
  - 失败明细下载与二次导入流程
