# 待完善任务清单

更新时间：2026-07-21

## 已完成（2026-07-21）
- 合同导出 EXCEL：
	- 前端合同列表新增“导出EXCEL”按钮。
	- 后端新增 `GET /api/contracts/export-excel`，按筛选条件导出“合同信息”单表。
- OCR Markdown 保存回写合同全文：
	- `PUT /api/html/<path>/full.md` 保存成功后，按路径匹配合同并更新 `fullbody`。
- 归档修改权限联动：
	- 已归档合同仅超管或群晖超管可进行更新/删除/上传等写操作。

## 已完成（2026-06-03）
- 全屏文件预览弹窗标题行新增目录面包路径（`/` 分隔）：
	- `frontend/src/components/ContractItem.vue`
	- `frontend/src/views/HomeView.vue`

## 1. 设置页 folder_options 稳定性
- 处理远程存储场景下 FileStation `119` 会话失效导致 `/api/settings/users` 偶发 500。
- 目标：文件夹选项读取失败时不阻断用户列表返回，降级为空列表并给出 warning。

## 2. 文件夹范围语义落地
- 明确“全部”在后端保存与鉴权中的语义（全目录或忽略文件夹限制）。
- 增加保存前标准化规则，避免“全部 + 具体目录”混存。

## 3. 用户权限页操作完善
- 增加防误操作策略（例如：限制删除当前登录用户，或增加更明确提示）。

## 4. 批量匹配能力增强
- 增加“匹配预演（不写库）”模式。
- 增加匹配策略开关：仅 `exact` 或允许 `contains-*`。
- 增加结果导出（CSV）便于审计。

## 5. 导入链路回归（Excel）
- 核对导入规则提示与后端真实校验一致。
- 使用真实 MIS 导出文件做一轮导入与失败明细验证。

## 6. 新字段回归（copy_count / save_place）
- 后端校验与接口返回一致性回归。
- 模板与导入场景（空值/合法/非法）回归。
- 前端展示、编辑、搜索回归。

## 7. 首页缓存与加载体验回归
- 缓存命中/过期/损坏场景验证。
- 后端异常时的降级显示与提示验证。

## 8. 本地开发体验整理
- 统一后端启动方式（脚本/任务/文档保持一致）。
- 清理与当前工程不一致的历史启动说明。

## 9. 新上传流程回归
- 回归 `ContractItem` 中“上传文件”改为“先选树形目录再选文件”的全流程。
- 覆盖新建态（仅回填 file_path）与编辑态（立即写回合同 file_path）两种路径。

## 10. AI 解析入口一致性回归
- 确认前端各入口调用 `/api/contracts/ai-parse` 均采用 `file_path`。
- 观察后端在仅 `file_path` 输入时的 OCR、超时、错误提示链路是否稳定。

## 11. OCR 落盘路径回归
- 校验 MinerU 结果落盘目录是否符合：`instance/ocr/<相对目录>/<文件名去扩展名>/`。
- 校验非法路径字符清洗与路径穿越防护是否有效。

## 12. 权限矩阵回归（新增）
- 校验“新增/删除”权限规则：仅 `super_admin` 或登录名 `zhangyan` 可操作。
- 覆盖用户权限页、部门设置页、项目设置页的按钮显隐与后端 403 返回。
- 额外验证非授权用户直接调用相关写接口时被拒绝。

## 13. OCR 预览链路回归（新增）
- 覆盖 `_origin.pdf` 存在/不存在两条路径，确认 `html-meta` 回退预览稳定。
- 验证受保护预览链接在慢网下不出现 `Missing token/404` 闪屏。

## 14. OCR 编辑器资源路径回归（新增）
- 验证编辑态相对路径图片、链接在 WYSIWYG 中可见且可点击。
- 验证保存后 markdown 仍为相对路径，不落地 `/api/html/...` 绝对接口地址。

## 15. 扫描目录工作台回归（新增）
- 回归 `ContractScanView` 左侧缩略图、右侧预览、导入合同完整链路。
- 验证首页“扫描仪”区域点击后能跳转到扫描页并自动选中目标文件。
- 验证扫描目录为空、预览失败、导入失败时的提示文案与降级行为。

## 16. 远程存储服务账号回归（新增）
- 使用“无 NAS 目录权限”的普通登录用户回归合同目录与扫描目录的读写能力。
- 覆盖文件档案、合同附件预览下载、扫描目录预览、扫描导入、批量匹配等远程存储访问链路。
- 确认用户/群组管理仍按真实 DSM 登录用户权限执行，不受服务账号切换影响。
